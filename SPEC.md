# 時間外勤務報告システム - 仕様書

## 1. システム概要

### 1.1 目的
社員が時間外勤務時間と振替時間をWeb経由で報告し、所属別・年月別に集計するシステム

### 1.2 システム構成
- **プラットフォーム**: Google Apps Script (GAS)
- **データストレージ**: Google スプレッドシート
- **アクセス方法**: Webブラウザ
- **タイムゾーン**: Asia/Tokyo
- **アクセス権限**: ANYONE_ANONYMOUS（匿名アクセス可）

---

## 2. 画面遷移フロー

```
[ログイン画面]
    ↓ (ログインID入力)
[メニュー画面]
    ↓
    ├─→ [時間外報告] → [年月選択] → [入力画面] → [メニュー画面]
    └─→ [時間外集計] → [集計メニュー]
                            ↓
                            ├─→ [所属別集計結果]
                            └─→ [年月別集計結果]
```

---

## 3. データ構造

### 3.1 スプレッドシート構成

#### **シート1: ユーザー名**
| 列 | 項目名 | データ型 | 説明 |
|----|--------|----------|------|
| A  | ログインID | 文字列 | ユーザーの一意識別子 |
| B  | 氏名 | 文字列 | ユーザーの氏名 |
| C  | 課 | 文字列 | 所属する課 |
| D  | 係 | 文字列 | 所属する係 |

**用途**: ログイン認証と所属情報の管理

#### **シート2: テーブル**
| 列 | 項目名 | データ型 | 説明 |
|----|--------|----------|------|
| A  | 年月 | 文字列/日付 | 報告対象の年月（例: "2025年1月"） |
| B  | ログインID | 文字列 | 報告者のログインID |
| C  | 時間外 | 数値 | 時間外勤務時間（時間単位） |
| D  | 振替時間 | 数値 | 振替時間（時間単位） |
| E  | 備考 | 文字列 | 備考・メモ（v2.0以降追加） |

**用途**: 時間外勤務実績の記録

#### **シート3: 年月**
| 列 | 項目名 | データ型 | 説明 |
|----|--------|----------|------|
| A  | 年月 | 文字列 | 選択可能な年月（例: "2025年1月"） |

**用途**: ~~年月選択画面のドロップダウンリスト~~ **※廃止** - 年月リストは自動生成に変更

**⚠️ 重要**: v2.0以降、年月リストは`年月リスト自動生成()`関数により自動生成されます。このシートは不要になりました。

---

## 4. 機能仕様

### 4.1 ログイン機能

#### **ファイル**: `login.html`

#### **処理フロー**
1. ページ読み込み時、localStorage から保存済みログインIDを取得
2. 保存済みIDがある場合、自動でログインフォームを送信（自動ログイン）
3. 手動ログインの場合、ログインボタン押下でIDを localStorage に保存後、フォーム送信

#### **認証処理** (`Routing.js` - `ログイン処理` 関数)
1. 入力されたログインIDを取得
2. スプレッドシート「ユーザー名」シートのA列（ログインID）と照合
3. 一致する場合、メニュー画面へ遷移
4. 不一致の場合、"Invalid login" メッセージを表示

#### **入力項目**
- **ログインID**: 必須、文字列

#### **自動ログイン仕様**
- localStorage のキー: `loginId`
- 保存タイミング: ログインボタン押下時
- 読み込みタイミング: ページ読み込み時（DOMContentLoaded）

---

### 4.2 メニュー機能

#### **ファイル**: `メニュー.html`

#### **表示項目**
- ログインユーザーのID表示
- 「時間外報告」ボタン
- 「時間外集計」ボタン

#### **ボタン動作**
- **時間外報告**: `action=時間外報告` で POST送信
- **時間外集計**: `action=時間外集計` で POST送信

---

### 4.3 時間外報告機能

#### 4.3.1 年月選択画面

##### **ファイル**: `select.html`

##### **処理フロー** (`TimeReport.js` - `handleTimeReport` 関数)
1. **年月リストを自動生成**（`年月リスト自動生成()`関数を使用）
   - 現在の年度を判定（4月〜3月基準）
   - 過去2年度 + 当年度 + 翌年度 = 合計4年度分（48ヶ月）を生成
   - フォーマット: "yyyy年M月"（例: "2023年4月"〜"2027年3月"）
2. スプレッドシート「テーブル」シートから既存データを取得
3. 各年月について、ログインユーザーの既存データを検索
4. 既存データがある場合、時間外・振替時間・備考の値を表示用に設定
5. 年月リストと既存値を select.html に渡して表示

##### **年月自動生成ロジック** (`年月リスト自動生成` 関数)
```javascript
// 現在日時から年度を判定
var currentFiscalYear = (currentMonth >= 4) ? currentYear : currentYear - 1;

// 過去2年度 + 当年度 + 翌年度 = 4年度分
for (fiscalYearOffset = -2 to 1) {
  // 各年度: 4月〜3月（12ヶ月）を生成
  年月リスト.push([fiscalYear + "年" + month + "月"]);
}
```

**利点**:
- 年度切り替え時にコード変更不要
- 常に最新の4年度分を自動表示
- 遡及入力・将来入力に対応

##### **表示仕様**
- 年月ごとにカード形式で表示
- 既存データがある場合、時間外・振替時間を表示
- 「編集」ボタンで入力画面へ遷移

##### **データ受け渡し**
```javascript
{
  年月: "2025年1月",
  B: 10.5,        // 既存の時間外値
  C: 2.0          // 既存の振替時間値
}
```

#### 4.3.2 入力画面

##### **ファイル**: `input.html`

##### **処理フロー** (`Routing.js` - `case "select"`)
1. select.html で選択された年月を受け取る
2. 既存の時間外・振替時間値を受け取る
3. input.html に値を設定して表示

##### **入力項目**
- **年月**: 表示のみ（hidden フィールドで送信）
- **時間外**: 数値入力、既存値があれば表示
- **振替時間**: 数値入力、既存値があれば表示

##### **送信処理** (`TimeReport.js` - `handleSendText` 関数)
1. 入力値を受け取る（入力ID, 年月, 時間外, 振替時間）
2. 「テーブル」シートから既存データを検索
3. **既存データがある場合**:
   - 該当行の C列（時間外）・D列（振替時間）を上書き
4. **既存データがない場合**:
   - 新規行を追加（年月, ログインID, 時間外, 振替時間）
5. 処理完了後、メニュー画面に戻る

##### **データ更新ロジック**
- **検索条件**: 年月（A列）とログインID（B列）が一致
- **日付フォーマット**: Date型の場合、"yyyy年M月" 形式に変換して比較

---

### 4.4 集計機能

#### 4.4.1 集計メニュー

##### **ファイル**: `集計メニュー.html`

##### **表示項目**
- 「所属別集計」ボタン
- 「年月別集計」ボタン

#### 4.4.2 所属別集計

##### **ファイル**: `Aggregation.js` - `handleShozokuBetsuSuikei` 関数

##### **処理フロー**
1. 「ユーザー名」シートからユーザー情報を取得（2行目以降）
2. ログインID → 所属情報（課・係）のマッピングを作成
3. 「テーブル」シートから全時間外データを取得
4. データを階層構造で集計:
   ```
   課 → 係 → 年月 → {時間外合計, 振替時間合計}
   ```
5. 集計結果を `所属別集計結果.html` に渡して表示

##### **集計ロジック**
```javascript
hierarchical = {
  "営業課": {
    "第一係": {
      "2025年1月": { 時間外: 15.5, 振替時間: 3.0 },
      "2025年2月": { 時間外: 12.0, 振替時間: 2.5 }
    },
    "第二係": { ... }
  },
  "総務課": { ... }
}
```

##### **表示仕様** (`所属別集計結果.html`)
- 課 → 係 → 年月の階層構造で表示
- 各階層で時間外・振替時間の合計を表示
- 所属情報がないユーザーは「未設定」として集計

#### 4.4.3 年月別集計

##### **ファイル**: `Aggregation.js` - `handleNengetsuBetsuSuikei` 関数

##### **処理フロー**
1. 「テーブル」シートから全時間外データを取得
2. 年月ごとに時間外・振替時間を集計
3. 集計結果を `年月別集計結果.html` に渡して表示

##### **集計ロジック**
```javascript
aggregationByYm = {
  "2025年1月": { 時間外: 50.5, 振替時間: 10.0 },
  "2025年2月": { 時間外: 45.0, 振替時間: 8.5 }
}
```

##### **表示仕様** (`年月別集計結果.html`)
- 年月ごとにテーブル形式で表示
- 各年月の時間外・振替時間の合計を表示

---

## 5. ルーティング仕様

### 5.1 GET リクエスト処理 (`doGet` 関数)

#### **パラメータ**
- `page`: ページ識別子（オプション）

#### **ルーティング**
| page値 | 遷移先 | 説明 |
|--------|--------|------|
| `input` | input.html | 入力画面 |
| `menu` | メニュー.html | メニュー画面 |
| (未指定) | login.html | ログイン画面（デフォルト） |

### 5.2 POST リクエスト処理 (`doPost` 関数)

#### **パラメータ**
- `action`: アクション識別子（必須）

#### **ルーティング**
| action値 | 処理関数 | 説明 |
|----------|----------|------|
| `login` | ログイン処理() | ログイン認証 |
| `時間外報告` | handleTimeReport() | 年月選択画面表示 |
| `時間外集計` | - | 集計メニュー表示 |
| `所属別集計` | handleShozokuBetsuSuikei() | 所属別集計実行 |
| `年月別集計` | handleNengetsuBetsuSuikei() | 年月別集計実行 |
| `select` | - | 入力画面表示（年月選択後） |
| `sendText` | handleSendText() | 時間外データ保存 |
| (その他) | - | ログイン画面表示 |

---

## 6. 技術仕様

### 6.1 Google Apps Script 設定

#### **appsscript.json**
```json
{
  "timeZone": "Asia/Tokyo",
  "dependencies": {},
  "webapp": {
    "executeAs": "USER_DEPLOYING",
    "access": "ANYONE_ANONYMOUS"
  },
  "exceptionLogging": "STACKDRIVER",
  "runtimeVersion": "V8"
}
```

### 6.2 使用API

- **SpreadsheetApp**: スプレッドシート操作
- **HtmlService**: HTML テンプレート処理
- **Logger**: ログ出力
- **Session**: タイムゾーン取得
- **Utilities**: 日付フォーマット
- **ScriptApp**: Web アプリ URL 取得

### 6.3 日付フォーマット

- **フォーマット形式**: `yyyy年M月`
- **タイムゾーン**: `Session.getScriptTimeZone()` (Asia/Tokyo)
- **使用メソッド**: `Utilities.formatDate()`

---

## 7. エラーハンドリング

### 7.1 ログイン失敗
- **条件**: ログインIDが「ユーザー名」シートに存在しない
- **処理**: "Invalid login" メッセージを表示

### 7.2 データ取得エラー
- **条件**: シートが空の場合
- **処理**: `getLastRow() > 1` でチェック、空配列で処理続行

### 7.3 所属情報未設定
- **条件**: ユーザー名シートに所属情報がない
- **処理**: 課・係を「未設定」として集計

---

## 8. セキュリティ仕様

### 8.1 認証
- **方式**: ログインIDによる簡易認証
- **パスワード**: なし
- **セッション管理**: localStorage による自動ログイン

### 8.2 アクセス制御
- **Web アプリアクセス**: `ANYONE_ANONYMOUS` （匿名可）
- **スプレッドシートアクセス**: デプロイユーザーの権限で実行
- **推奨**: 本番環境では適切なアクセス制限を設定

---

## 9. UI/UX仕様

### 9.1 デザイン
- **フォント**: Arial, sans-serif
- **背景色**: #f4f4f4（グレー）
- **カラーテーマ**: 緑系（#4CAF50）
- **レイアウト**: フレックスボックス、中央配置

### 9.2 レスポンシブ対応
- **未対応**: 固定幅レイアウト
- **推奨デバイス**: PCブラウザ

### 9.3 select.html の特殊仕様
- **フォント**: Google Fonts - Roboto
- **デザイン**: グラデーション背景、カードUI
- **ホバーエフェクト**: あり

---

## 10. ログ仕様

### 10.1 ログ出力箇所
- ルーティング処理（doGet, doPost）
- 各アクション処理の開始時
- データ取得・集計結果

### 10.2 ログレベル
- **使用メソッド**: `Logger.log()`
- **出力先**: Stackdriver Logging

### 10.3 ログ例
```
doGet called, page: input
doPost action: 時間外報告
時間外報告 action triggered for user: user01
年月リスト: [["2025年1月"],["2025年2月"]]
```

---

## 11. 制限事項・注意点

### 11.1 データ制限
- スプレッドシートの行数制限に依存
- 大量データでのパフォーマンス未検証

### 11.2 同時アクセス
- GAS の同時実行制限に依存
- 排他制御なし（上書き衝突の可能性あり）

### 11.3 ブラウザ依存
- localStorage 使用（プライベートブラウズモード非対応）
- モダンブラウザ推奨（ES5+）

### 11.4 日付処理
- Date型と文字列型の混在に対応
- "yyyy年M月" 形式での文字列比較

---

## 12. 今後の拡張可能性

### 12.1 機能追加候補
- パスワード認証
- 承認ワークフロー
- データエクスポート機能
- グラフ表示
- 通知機能

### 12.2 改善候補
- レスポンシブデザイン対応
- エラーメッセージの日本語化
- データバリデーション強化
- 排他制御の実装

---

**作成日**: 2025年11月22日
**バージョン**: 1.0
